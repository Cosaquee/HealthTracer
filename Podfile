# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HealtTracer' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HealtTracer
  pod 'MaterialComponents/Cards'
  pod 'RealmSwift'
  pod "RealmGeoQueries"
  pod 'PopupDialog', '~> 0.7'

  target 'HealtTracerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HealtTracerUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
